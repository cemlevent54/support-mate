const authProxy = require('./auth.proxy.js');
const ticketProxy = require('./ticket.proxy.js');
const notificationProxy = require('./notification.proxy.js');

module.exports = {
  authProxy,
  notificationProxy,
  ticketProxy
};

version: "3.8"
services:
  redis_auth_service:
    image: redis:latest
    container_name: redis_auth_service
    ports:
      - "6379:6379"
    restart: always
    volumes:
      - ./data:/data
    networks:
      - redis-network
    command: redis-server --appendonly yes

  # Redis Admin UI (Opsional)
  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: redis_commander
    ports:
      - "5002:8081"
    environment:
      - REDIS_HOSTS=local:redis_auth_service:6379
    depends_on:
      - redis_auth_service
    networks:
      - redis-network
    restart: always

volumes:
  redis_data:

networks:
  redis-network:
    driver: bridge 